apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-chat
spec:
  rules:
  - host: my.kubernetes.example
    http:
      paths:
      - path: /chat
        backend:
          serviceName: bapp-svc
          servicePort: 80
      - backend:
          serviceName: fapp-svc
          servicePort: 80
