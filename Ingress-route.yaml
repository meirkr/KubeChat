apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-chat
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: service-frontend-80
          servicePort: 80
      - path: /chat
        backend:
          serviceName: service-backend-80
          servicePort: 80