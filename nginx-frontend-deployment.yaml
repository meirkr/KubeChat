apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: frontend-app
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: frontend-app
    spec:
      containers:
      - name: frontend-app
        image: meirkr/blazor_chat:latest
        ports:
        - containerPort: 80